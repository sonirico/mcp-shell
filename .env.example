# Server Configuration
MCP_SHELL_SERVER_NAME=mcp-shell 🐚
MCP_SHELL_VERSION=dev

# Logging Configuration
MCP_SHELL_LOG_LEVEL=info
MCP_SHELL_LOG_FORMAT=console
MCP_SHELL_LOG_OUTPUT=stderr

# Configuration File Path
MCP_SHELL_CONFIG_FILE=config.json

# Security settings are configured only in JSON config file
# See config.example.json for security configuration options
