# Secure mode (default) exposes typed tools only: file, git and
# operator-defined scripts. Every path is confined to working_directory.
# shell_exec exists only when MCP_SHELL_ALLOW_UNSAFE=1 and is unrestricted by
# design.
security:
  # Enable security features
  enabled: true

  # Execution limits
  max_execution_time: "30s"
  max_output_size: 1048576  # 1MB

  # Security context
  working_directory: "/tmp"
  run_as_user: ""  # Leave empty to run as current user

  # Logging
  audit_log: true

  # Expose file and git write tools (write_file, edit_file, mkdir, move,
  # delete, git_add, git_commit, git_switch, git_restore, git_stash). Off by
  # default.
  writes_enabled: false

  # Operator-defined scripts exposed through the run_script tool. The client
  # picks a name; the argv is yours and cannot be altered.
  # scripts:
  #   test: ["go", "test", "./..."]
  #   lint: ["golangci-lint", "run"]
